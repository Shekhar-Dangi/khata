node_modules/
.env
CLAUDE.local.md
# Real bank data never leaves this machine. `data/` WHOLESALE, not just data/private/:
# the old rule meant a statement dropped one directory up would have been committed,
# and for this repo that mistake is not recoverable once pushed.
data/
.DS_Store
ingest/.venv/
__pycache__/
*.pyc

# Refactor safety net — regenerated on demand, never committed.
.snapshots/
