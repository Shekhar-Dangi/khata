# Copy to .env and adjust. .env is gitignored — never commit real values.
DATABASE_URL=postgresql://USER@localhost:5432/finance
PORT=3000

# Which person column in a Splitwise export is yours (exact header text)
SPLITWISE_ME=
